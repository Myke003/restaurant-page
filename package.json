{
  "name": "restaurant-page",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "~5.7.2",
    "vite": "^6.3.1"
  },

  "scripts": {
  "dev": "vite",            // Para desarrollo
  "build": "vite build",    // Para producción
  "preview": "vite preview", // Para ver la build localmente
  "deploy": "gh-pages -d dist"  // Despliega la carpeta dist a GitHub Pages
  }

}
